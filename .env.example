# Copy this file to .env and fill in your values.
# .env is gitignored and never committed.

# Optional: absolute host path where monthly note backups are mirrored.
# Leave empty to disable the extra copy (backups still go to the Docker volume).
EXTRA_BACKUP_PATH=/home/youruser/file-storage/information
